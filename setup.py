from setuptools import setup, find_packages

setup(
    name="limina-ai",
    version="1.0.0",
    description="Python SDK for Limina AI — Trajectory Diagnostics & Automated Prompt Patching",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)